{
  "name": "jaldrishti-site",
  "version": "1.0.0",
  "private": true,
  "description": "JalDrishti — wastewater surveillance showcase site (static)",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
